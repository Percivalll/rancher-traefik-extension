# Traefik

Manage Traefik Proxy `v3.7.0` Kubernetes CRDs directly from Rancher Dashboard.

The extension adds navigation and CRD views for HTTP routing, middleware, advanced Traefik services, backend transport security, TLS policy, and Layer 4 routing resources.

Install Traefik CRDs in the cluster before using these resource pages.
